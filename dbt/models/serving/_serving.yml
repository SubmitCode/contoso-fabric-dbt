version: 2

models:
  - name: europe_fct_sales
    description: "Serving view — fct_sales filtered to European customers (continent = 'Europe')"
    columns:
      - name: order_key
        tests:
          - not_null
      - name: continent
        tests:
          - not_null
