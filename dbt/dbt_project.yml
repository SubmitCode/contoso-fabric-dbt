name: contoso_fabric_dbt
version: '1.0.0'
config-version: 2

profile: contoso_fabric_dbt

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  contoso_fabric_dbt:
    +schema_suffix_connector: ''
    silver:
      +materialized: table
      +schema: silver
    gold:
      +materialized: table
      +schema: gold
    serving:
      +materialized: view
      +schema: serving
